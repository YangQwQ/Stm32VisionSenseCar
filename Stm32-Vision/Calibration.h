#pragma once
#include "src/exec/bivar.h"   // bivar::CalPt / ArmSet 类型（机械臂散点表）
#include <stdint.h>

// =====================================================================
//  手动校准数据集中区（真机实测）。改这里即可，别散落到各处源文件里。
//  涵盖：舵机限位&机械臂参数、小车移动时长近似（定距/定角）、
//        机械臂夹心坐标散点（IDW）、屏幕→地面单应标定点。
//  均为真机实测值：改值即改行为；调整参数前先确认已实测。
// =====================================================================

// ---- 舵机限位 / 回中（大圣机械臂_NeZha 三舵机，勿随意改） ----
// Servo2=移爪  Servo3=夹爪  Servo4=抬落；Servo1=转向。
// ⚠️ 正前 2026-09-22 又调到 147（145 实测略右斜、150 也是右斜）：147 目测更正，但用户自己也
// 摆不十分正，属±2 内尝鲜值。120/180 仍是左右限位。回中偏几度的后果是 steering=0 直线 move
// 斜着走，短距到点判定被斜向位移带偏（实测 goto(30,30) 到 (40,30)，X 超 10cm）。
inline constexpr int STEER_CENTER = 147, STEER_LO = 120, STEER_HI = 180;     // 转向：正前/左/右
inline constexpr int REACH_CENTER = 200, REACH_LO = 120, REACH_HI = 250;     // 移爪
inline constexpr int GRIP_CENTER = 140, GRIP_CLOSE = 50, GRIP_LO = 50, GRIP_HI = 140;  // 夹爪
inline constexpr int LIFT_CENTER = 180, LIFT_LO = 115, LIFT_HI = 250;        // 抬落

// ---- 机械臂连续动作 / 折叠 / 缓动 ----
inline constexpr float ARM_STEP_CM = 0.5f;   // 每拍夹心平动 cm ≈ 50cm/s（loop 约 10ms 一拍）
inline constexpr int   ARM_CNT_PER_CM = 2;   // 定距 dist_cm 折算拍数（粗近似）
inline constexpr int   FOLD_LIFT_PWM = 130, FOLD_REACH_PWM = 130;  // 收臂折叠姿态
inline constexpr float ARM_SMOOTH_PEAK = 2.0f;  // S 形缓动峰值 PWM/拍
// 连续步进"原地无进展"容忍拍数：到物理限位后 FK 回读不再变化，但 arm_pose 仍在被反复重解，
// 最高位附近散点稀疏、IDW 反解会在两个解之间来回跳 → 舵机嗡嗡抽搐（实测只在抬到最高位时出现）。
// 连续 N 拍 FK 位移都小于 ARM_STALL_EPS_CM 即判到边界并主动停，不再空推。约 50ms/拍。
inline constexpr int   ARM_STEP_STALL_N = 8;
inline constexpr float ARM_STALL_EPS_CM = 0.05f;

// ---- 低姿夹取准备位（arm 指令 act="low" 的落点）----
// 夹心 x=车头前 cm / h=离地 cm。取可达域里"够低、又离车身够远"的一个**实测点**（kArmPts 弯带在
// 低处很窄），于是它反解出来的 pwm 就是真值，不靠外插。用法：先把爪降到这儿，再靠车的前后移动
// 把目标送进两指之间 —— 比让上层去猜一个够不着的 (x,h) 可靠（猜错的代价是空夹且无任何报错）。
// ⚠️ 重新标定 kArmPts 后必须连同复核本值（以及按它推出的"目标应落在画面 v≈0.62~0.69"）。
// ⚠️ 这两个数是**请求值**，不是落点：低处弯带边缘上"请求 (7.0,0.5) 实际只落到 (7.8,0.8)"这类
// 偏差是常态（FK∘IK 在稀疏点处不互逆），而状态行会把偏差报成"位姿没到位"、让 AI 以为动作失败。
// 现值是**实机逐点核过、落点无诊断**的一组：(8.0,1.0) → 实际落 (8,1)，pwm 170/232。
inline constexpr float ARM_LOW_X_CM = 8.0f;
inline constexpr float ARM_LOW_H_CM = 1.0f;
// 固定抬臂位（arm 指令 act="raise" 的落点）：夹取后验证/看清爪下时一次到位（S 形缓动，
// 非持续 lift_up 步进，天然不会在边界抽搐）。低姿位/折叠位之外的第三个固定位，实机复核后调。
inline constexpr float ARM_RAISE_X_CM = 9.0f;
inline constexpr float ARM_RAISE_H_CM = 9.0f;
// 合爪后自动抬臂（arm 指令 act="grasp"）：合成一个动作省一轮往返，且抬升有界 —— 连续 lift_up
// 抬到最高位会在机械止点附近抽搐（见 ARM_STEP_STALL_N），定量抬升天然避开。
inline constexpr float GRASP_LIFT_CM = 7.0f;  // 合爪后抬起的高度
inline constexpr int   GRASP_SETTLE_MS = 250;  // 合爪到抬臂之间的等待（让夹爪真合到位再抬）
// 低姿下**爪口中心相对车头轴线的横向偏置** cm（正=偏右）。机械臂只有前后+抬落两个自由度、横向
// 为零，故"目标横向要对到哪儿"必须以爪口为准，而不是车头轴线 —— 摄像头与爪同装一车，爪口在画面
// 里固定偏在一侧（提示词规则 (a) 写作"夹取低姿下它在画面中心偏右那一带"），两者就是同一个事实。
// 现值由两处证据估得：提示词 (a) 的方向 + 日志里 AI 两次报"方块停在爪口左侧"而闸门当时按
// |横向|≤容差放行（那两次的横向读数约 -1cm）。**这是估计值，不是精测值**：闸门拿它算横向误差并
// 给出该转几度，值偏了就直接转偏。重标 kArmPts / 动过摄像头或机械臂安装后必须重测（测法：把目标
// 摆到车头轴线上，arm low 后看方块落在爪口哪一侧、差多少）。
inline constexpr float ARM_LOW_LAT_CM = 1.0f;

// ---- 原地旋转的车身几何（AI 姿态累积用）----
// 坐标系原点=车头，但四轮差速原地旋转的枢轴是车几何中心、不是车头原点——于是旋转时车头原点
// 本身会移动。car_update_pose 拿车头相对枢轴的距离折算这段位移，否则旋转后记忆坐标整体漂移
// （实测原地转 90° 车头漂到 (5,-5)~(5,-6.5)，方向=右转右后/左转左后，与刚性枢轴模型一致）。
// 纯刚性按轮距中心算（车前 15.5cm 全长→8.25cm）会高估约 1.4 倍（模型 11cm vs 实测 8cm），
// 存在滑移，取经验折中值（5~7cm 区间都试过，方向与量级对得上）。
inline constexpr float SPIN_PIVOT_BEHIND_CM = 6.0f;

// ---- 小车移动时长近似（电机无编码器，只能按时长模拟距离/角度） ----
// 移动 actual_cm ≈ v(throttle)·t_s + c(throttle)，按油门插值；原地转角用时 ms/度随转速骤升。
inline constexpr int   SPIN_MIN_SPEED = 800;   // 低于此转速原地旋转拖不动，抬升到可靠值
// 持续旋转（无定角）单次时限折算的最大角度：一圈足够环视，再多只是空烧（车尾还挂着充电线，缠住就走不动）。
inline constexpr int   SPIN_CONT_MAX_DEG = 360;
// ⚠️ 0.15 与 0.25 两档是 2026-09-22 真机实测重定的（各 cm=2/4/6/9 正反各一次，共 16 个样本，
// 用板端同一单应反投影量位移）。这两档此前**全无实测依据**，而它们恰好是 AI 的唯一工作区间：
// AI 近场发的油门是 0.1~0.25（实测 0.1/0.2），近场闸门更是把油门钉在 AI_NEAR_STEP_TH=0.25。
//
// 模型 actual = v·max(0, t − 死区)，分别拟合：
//   th=0.15 独拟合 v=6.60 B=354ms RMSE 0.46cm（8 点，拟合很干净）
//   th=0.25 独拟合 v=8.98 B=380ms RMSE 1.51cm（8 点，散布大，单独不足以定值）
//   两档合并   v=7.80 B=370ms RMSE 1.24cm  ← 0.25 档采用这个（合并拟合、借 0.15 档的强度）
// 依据：**同一个 ms 下两档的实测位移只差噪声**（都落在各自散布内）⇒ 该区间"油门"几乎不影响结果，
// 结果由起步死区主导，两档本就统计上分不开，没理由给它们差 56% 的 v。
// 旧值 10.3 让 2cm 指令只发 514ms、真走 ~1.1cm（残差最大的那个样本只走 0.16cm）—— 近场"时而不动、
// 时而冲过头"的来路：AI 据记忆以为已顶到位（car_update_pose 按 distance_cm 累加），实际没动。
// 0.5/1.0 档有各自的实测依据（见下面 c 的注释），未动、也别外推。
inline constexpr float MV_SPEED_X[] = { 0.15f, 0.25f, 0.5f,  1.0f };
inline constexpr float MV_SPEED_Y[] = { 6.6f,  7.8f,  11.3f, 12.5f };
inline constexpr float MV_COAST_X[] = { 0.15f, 0.25f, 0.5f,  1.0f };
// ⚠️ 0.5 档 2026-09-22 实测定距(理论:实际): 10:12 / 20:21.5 / 30:32.25 / 40:42 ⇒ 恒定超出约 1.75cm
// (拟合 实际≈理论×1.0075+1.75, 斜率已准, 主要是起停余量偏小)。1.7 → 3.45。其余档未重测, 别外推。
inline constexpr float MV_COAST_Y[] = { 0.0f,  0.0f,  3.45f, 1.75f };
inline constexpr int   MV_N = (int)(sizeof(MV_SPEED_Y) / sizeof(MV_SPEED_Y[0]));
// 定距脉冲的**起步死区**与**脉冲下限**（ms）。v/c 表是在**长脉冲**上拟合的，短脉冲会整段落进死区：
// 实测（红灯斑点位移标定，throttle 0.35）指令 3cm(217ms)、5cm(404ms) 整车纹丝不动，8cm(684ms) 起才动，
// 且每段都比指令少走 2~3.5cm —— 即每段脉冲开头都有一截"电机通电但车不动"的时间。
// ⚠️ 危害不是"走不准"而是**同源不一致**：`car_update_pose` 按 distance_cm 累加车位移，于是 AI 记忆里
// "已经小步顶到位"、实际原地没动，它据此下探合爪 → 对着空气夹（实测空夹的主要来路）。提示词与近场
// 闸门都要求"近场单步≤2cm"，那个步长此前恰好整个落在死区里 —— 等于把最后 2cm 的对准变成空转。
inline constexpr int   MV_START_MS = 320;      // 死区补偿：每段定距脉冲加上这段（它不产生位移）
inline constexpr int   MV_MIN_PULSE_MS = 500;  // 脉冲下限：宁可多走一两厘米，也不能"指令走了、车没动"
// 原地旋转：**角度→通电ms 查表插值**（spin_ms 直接测，重复执行累计到 90° 取平均）。
// 实测曲线(用户 2026-09-24, spin_ms 直测)：
//   t(ms)    1    10   15   25   50   100   500   750   2250  4500
//   deg    0.96  2.2 3.25 4.5 6.43 10.56   40    60  173.1 337.5
// 每度所需ms单调上升(启动瞬间快 ~1ms/°, 稳态 ~13ms/°) ⇒ 单斜率+滑行角公式无法拟合，查表最直接。
// 表内角升序；目标角在表内线性插值、超出末点用末段斜率外推。0 点隐含 (0,0)。
inline constexpr int   SPIN_TBL_N = 10;
inline constexpr float SPIN_TBL_DEG[SPIN_TBL_N] = { 0.0f, 2.2f, 3.25f, 4.5f, 6.43f, 10.56f, 40.0f, 60.0f, 173.1f, 337.5f };
inline constexpr int   SPIN_TBL_MS[SPIN_TBL_N]  = { 0,   10,    15,    25,   50,    100,    500,    750,    2250,   4500 };
// ⚠️ 上述曲线在特定电池电压/地面下测得；电压下降转速变慢，长时漂移需重测(或走 mvfy 闭环)。

// ---- 运动到位验证（motion_verify）软解缩略图与检测阈值 ----
// 编译级总开关：置 0 可在不删代码的情况下完全禁用 mvfy（不创建任务、不参与取帧），
// 用于 A/B 排查"开始并行取帧后图传更卡/FB-OVF"是否由 mvfy 引入。默认开，排查时临时置 0 复测。
#ifndef MVFY_ENABLED
#define MVFY_ENABLED 1
#endif
// jpg2rgb565 软解缩放档：0=原尺寸 / 1=1/2 / 2=1/4 / 3=1/8。1/4 在分辨率与耗时间较均衡（VGA→160×120）。
inline constexpr int   MVFY_SCALE         = 2;     // JPEG 软解缩放档（esp_jpeg_image_scale_t）
// 采样节流（两帧间最小间隔 ms）：这只是"最快能多快"，实际节奏由抓帧+VGA 软解耗时决定
// （实测每拍 200~500ms，看日志的"抓/解"两列）。故它只占一小部分，调小是白捡一点、不是关键；
// 关键在软解档 MVFY_SCALE。单拍位移要按**实际**节奏估，别按这个值估。
inline constexpr int   MVFY_SAMPLE_MS     = 10;
// 判"画面到底动没动"的灰差门槛（灰度 0..255）。**这个值必须实测标定，凭直觉给必错**——
// 它同时决定 move 的受阻停车、与 spin 的"画面已变却报零位移,疑重复花纹"交叉校验。
// 2026-09-22 实测（场景：车被墙卡住、命令后退共 55cm 而画面对比证明实际位移≈0）：
//     0 像素位移（真静止 / 被卡住，含轮子空转的车身抖动）  d = 8.9 ~ 15.8
//     位移 1 缩略图像素（真在动的最低量）                  d = 20.0
//     位移 2~3 像素（车的正常速度：约 10cm/s × 0.34s/拍）   d = 24 ~ 27
// 门槛就取在这两个总体之间。⚠️ 旧值 2.5 比静止底噪还低 3.5 倍 ⇒ 恒判"在动" ⇒
// move 的受阻停车曾是**死代码**（车被墙顶住空转磨满 3.2s 上限也没触发过一次），
// spin 那条提示同理恒误报。改这个值前先按上面口径重测，别只调数字。
inline constexpr float MVFY_DIFF_LO       = 18.0f; // 相邻采样灰差均值低于此值视为"画面没动"
inline constexpr int   MVFY_BLOCKED_N     = 3;     // move：连续 N 拍"没动"即判受阻（约 1.0s）
// 起步缓冲：只用来压住「受阻/卡死」的误判（起转瞬间画面在抖/糊，别急着判没动）。
// ⚠️ 不要再拿它跳过转角累计：800 转速下 400ms ≈ 14 度，漏掉这段会让实测角系统性偏小，
// 补偿便按"还差 14 度"多补一段 —— 表现为转得比不验证更久、更过头。
inline constexpr int   MVFY_START_GRACE   = 400;   // 起步缓冲 ms（仅抑制"卡死"判定，不影响角度累计）
inline constexpr int   MVFY_SPIN_N        = 3;     // spin：连续 N 拍匹配失败/角度≈0 判"没转/被卡"
inline constexpr int   MVFY_SPIN_PX_TOL   = 2;     // spin：dx/dy 像素幅值均 ≤ 此值视为"画面无位移"（静止噪声漂移 1~2px，勿用严格 ==0）
// 匹配搜索半径（px）：只能覆盖"单拍位移"那么大，必须按实测节奏留足余量，否则最佳点顶到窗边。
// 实测（2026-09 日志）：本机 ROI 处约 1.1~1.6 px/度（160×120 缩略图，由 kGroundCal 单应反算），
// spin 800 转速实车约 70 度/秒，而每拍要抓帧 + VGA 软解（约 200~400ms，采样节流不是瓶颈）
// → 单拍位移约 15~36px。旧值 8/16 全都盖不住：argmin 贴着窗边，累计值表现为"每拍恒为
// 3~7 度"（= 被削顶的窗边上界），快转时还会落到重复纹理上忽正忽负。
// 按最坏节奏（一拍 400ms）仍有余量取 48：代价是 SSD 规模 (2·48+1)² × 16² ≈ 2.4M 次/拍
// （约 30~60ms，跑在空闲核心上、相对软解耗时可接受）。实测值看日志「mvfy spin拍」那行的
// dx/dy 与 dt：若出现"位移顶搜索窗"，说明还差，继续加大；若位移常在 10px 内，可回调省时间。
inline constexpr int   MVFY_MATCH_SR      = 48;    // 匹配搜索半径（px，越小越快、越大越抗快转）
// 基准块边长（px）：块太小（12）在大搜索窗里容易撞上重复纹理而匹配到错位置；
// 放大到 16 用更多纹理约束 argmin。代价是 SSD 计算量按面积涨（sr=32 时约 1M 次/拍，几十 ms 可承受）。
inline constexpr int   MVFY_PATCH_PX      = 16;    // 基准块边长（px）
inline constexpr float MVFY_PATCH_VAR_LO  = 25.f;  // 基准块灰度方差下限：低于此值=无纹理，匹配无意义
// 匹配残差 / 基准块纹理能量 的上限。这是"乱匹配"的宽松档：真匹配的残差是两帧间视角/模糊差异，
// 远小于纹理能量自身；残差都超过纹理能量说明没匹配上（重复纹理/纯色/被遮挡）。别调紧到 0.5 以下——
// 会连正常帧一起挡掉，样本数不够反而静默退回开环（等于没验证），排查时先看日志里的 n。
inline constexpr float MVFY_MATCH_COST    = 1.0f;
// 每拍原始匹配结果日志（dt/位移/匹配质量/判定）：调 mvfy 参数时置 1，看日志里单拍位移有多大、
// 有多少拍被哪一条门槛挡掉。参数定稳后置 0 减少刷屏。
// 已置 0：每条指令几十行采样日志会把真正的结论行淹掉（复盘时按签名 grep 的效率全被它拖垮）。
inline constexpr int   MVFY_LOG_SAMPLES    = 0;
// 帧龄（抓帧时相机打的时间戳 → 本拍取用的间隔）分两档：
//   FRESH = 排空兜底门槛：排空以"帧龄落进一个出帧周期内"为停手条件，出帧周期由相邻两抓的曝光差
//           现测（见 motion_verify 的 grab_thumb）；测不出周期时才退回这个固定值。取值 ≈ 本机一个
//           出帧周期（十几帧/秒），大一点只是多排一两帧，小一点会排到空队列去阻塞等新帧。
//   MAX   = 作废门槛：排空到头仍大于它 → 这帧确实是积压/停摆的旧帧，**跳过该拍**（不判、不计累计、
//           不当基准）。旧帧里画面自然"没动"，喂进匹配只会得出"没转"的假结论，还会误置
//           "画面已静止"让 exec 提前收沉降窗口、误清卡死计数。
// 为什么非排空不可：帧队列是 FIFO（先出最旧）且相机只在缓冲被取走后才出新帧，于是"每拍只取队首"
// 时画面每次只推进约一个出帧周期，而墙钟走了一整拍——日志实证 503ms 的一拍里两帧只差 47ms。
// 后果是实测角系统性偏小（45° 只测到 11°），且累计角覆盖不到停轮时刻（沉降窗口都结束了影像还没追上）。
// 病态侧实测见过 6.4s / 42s / 109s（空闲期队列冻结），故 MAX 取健康侧与病态侧之间。真值看日志「帧龄」列。
inline constexpr int   MVFY_FRAME_AGE_FRESH = 80;
inline constexpr int   MVFY_FRAME_AGE_MAX  = 800;
// 单拍最多抓几次帧（排空用）。帧队列 FIFO（先出最旧），队列深 = 帧缓冲数（camera.cpp 的 fb_count=3）
// → 最多排掉 3 帧才轮到新帧，故取 4；改了 fb_count 就要同步改这里。
// 顺带把积压缓冲还回相机，这正是抓帧模式 WHEN_EMPTY 下相机恢复供帧的前提。
// 何时会排空到底：mvfy 空闲时无人取帧，相机填满队列即停摆，队列里那几帧就此定格（实测见过帧龄 109s）；
// 那时最后一抓会阻塞等相机出新帧（一个出帧周期），换来的是"基准帧尽量贴近起转时刻"。
inline constexpr int   MVFY_GRAB_TRIES     = 4;
// （旋转到位补偿那一套宏 —— MVFY_SPIN_TOL_DEG / MVFY_SPIN_MAX_COMP / MVFY_SPIN_COMP_ENABLED /
//   MVFY_RATE_LO|HI_RATIO / MVFY_SPIN_SETTLE_MS —— 已整段删除。原因见 direct_exec.cpp 里
//   s_spin_goal 上方的注释：mvfy 块匹配在白地砖上测角不可靠，补偿会朝错方向追加旋转。）
inline constexpr int   MVFY_TASK_CORE     = 0;     // mvfy 软解独立任务所在核心（loop 在核心1，核心0 空闲）
inline constexpr int   MVFY_TASK_STACK    = 16384; // mvfy 任务栈（jpg2rgb565 软解+串行匹配，给足防栈 canary）

// ---- 屏幕→地面 单应标定点（改镜头/移相机后重测此表） ----
// 每行一个坐标对：(屏幕归一化 u, v) → (车头系地面 x右+, y前+ cm)。
// 屏幕归一化：左上(0,0)、右下(1,1)；纵轴越往下越近（v 大→近），横轴 u 小→车左。
// 锚点参考：屏幕中心 (0.5,0.5)→车前正对 (0,52)；下方中线 (0.5,0.75)→车前偏左 (-8,21)。
inline constexpr double kGroundCal[][4] = {
  {0.0,  0,  -89.5,   129},
  {0.0,  0.25,  -44,   40},
  {0.0,  0.5,   -31,   15.5},
  {0.0,  0.75,  -24,   3.5},
  {0.0,  1.0,   -21.5, -2.5},
  {0.25, 0,     -44,   147},
  {0.25, 0.25,  -22.5,   42},
  {0.25, 0.5,   -16, 15.5},
  {0.25, 0.75,  -13.5,   3},
  {0.25, 1.0,   -12,  -4},
  {0.50,  0,  20,   150},
  {0.50, 0.25,   2.5,    44},
  {0.50, 0.5,    0,    15.5},
  {0.50, 0.75,  -2,    2},
  {0.50, 1.0,   -5.5,   -4.5},
  {0.75, 0,   95,   170},
  {0.75, 0.25,   36.5,   48.5},
  {0.75, 0.5,    19.5,   14},
  {0.75, 0.75,   10,  1.5},
  {0.75, 1,   2.5,  -8},
  {1.0,  0.25,   75,   48},
  {1.0,  0.5,    37.5,   13.5},
  {1.0,  0.75,    24.5,   0},
  {1.0,  1,    14.5,   -8}
};
inline constexpr int kGroundCalN = (int)(sizeof(kGroundCal) / sizeof(kGroundCal[0]));

// ---- 机械臂夹心坐标标定散点（IDW 数据源，只读 flash） ----
// 每个点 = (reach_pwm, lift_pwm) → 实测夹心 (x车头前向cm, h离地cm)。
// 覆盖"能正常线性移动"的可达域，超出此域的动作移动不明显、由范围判定拒绝。
// 格式：{r=移爪, l=抬落, x车前, h离地}
inline constexpr bivar::CalPt kArmPts[] = {
  {140, 230, 7.0,  0.5},
  {150, 230, 7.75, 0.75},
  {160, 230, 7.0,  0.5},
  {170, 230, 8.0,  1.0},
  {180, 230, 10.0, 1.0},
  {190, 230, 11.0, 1.5},
  {130, 220, 7.5,  1.0},
  {140, 220, 8.25, 1.25},
  {150, 220, 9.0,  1.5},
  {160, 220, 10.0, 2.0},
  {170, 220, 10.5, 2.25},
  {180, 220, 11.5, 3.0},
  {190, 220, 12.0, 3.5},
  {200, 220, 12.25,3.75},
  {140, 210, 8.0,  1.5},
  {150, 210, 9.0,  2.0},
  {160, 210, 9.5,  2.75},
  {170, 210, 10.0, 3.25},
  {180, 210, 10.0, 4.0},
  {190, 210, 12.0, 4.75},
  {200, 210, 12.0, 5.25},
  {210, 210, 13.0, 6.75},
  {220, 210, 14.0, 7.25},
  {230, 210, 14.0, 8.5},
  {240, 210, 14.5, 8.75},
  {140, 200, 7.0,  3.5},
  {150, 200, 8.0,  4.25},
  {160, 200, 8.5,  4.75},
  {170, 200, 9.0,  4.75},
  {180, 200, 10.5, 5.0},
  {190, 200, 11.0, 6.0},
  {200, 200, 12.0, 6.75},
  {210, 200, 12.5, 7.25},
  {220, 200, 13.0, 8.0},
  {140, 190, 7.75, 5.0},
  {150, 190, 8.0,  5.25},
  {160, 190, 8.25, 5.5},
  {170, 190, 9.0,  5.75},
  {180, 190, 9.5,  6.0},
  {190, 190, 10.5, 6.75},
  {200, 190, 11.5, 7.5},
  {210, 190, 12.25, 8.5},
  {220, 190, 12.5, 8.5},
  {230, 190, 12.5, 9.25},
  {140, 180, 8.0,  5.25},
  {150, 180, 8.0,  6.0},
  {160, 180, 8.0,  6.25},
  {170, 180, 8.5,  6.5},
  {180, 180, 8.5,  7.0},
  {190, 180, 9.5,  7.5},
  {200, 180, 10.0, 8.0},
  {210, 180, 12.0, 9.25},
  {220, 180, 12.5, 10.0},
  {230, 180, 10.5, 8.5},
  {140, 170, 7.0,  6.25},
  {150, 170, 7.0,  6.5},
  {160, 170, 7.5,  7.0},
  {170, 170, 7.75, 7.25},
  {180, 170, 8.25, 7.5},
  {190, 170, 9.0,  8.25},
  {200, 170, 9.25, 9.0},
  {210, 170, 10.0, 9.75},
  {220, 170, 10.75,10.0},
  {140, 160, 5.0,  7.0},
  {150, 160, 6.0,  7.25},
  {160, 160, 7.25, 7.75},
  {170, 160, 8.0,  8.25},
  {180, 160, 8.5,  8.5},
  {190, 160, 8.75, 8.75},
  {200, 160, 10.0, 9.75},
  {210, 160, 10.0, 10.25},
  {220, 160, 10.0, 11.0},
  {230, 160, 11.5 ,12.5},
  {140, 150, 4.75, 7.75},
  {150, 150, 5.25, 7.5},
  {160, 150, 5.75, 8.25},
  {170, 150, 5.5,  8.5},
  {180, 150, 5.5,  9.0},
  {190, 150, 7.25, 9.25},
  {200, 150, 8.75, 10.0},
  {210, 150, 9.5,  11.0},
  {220, 150, 9.5,  11.5},
  {190, 140, 6.0,  9.75},
  {200, 140, 6.5,  10.0},
  {160, 130, 4.0,  9.5},
  {170, 130, 5.0,  9.25},
  {190, 130, 5.0,  9.5},
  {200, 130, 5.5,  10.5},
  {210, 130, 6.0,  11.0},
  {220, 130, 7.0,  12.0},
  {230, 130, 7.5,  12.5},
  {170, 100, 2.5, 9.75},
  {190, 100, 3.0, 10.5},
  {210, 100, 3.0, 11.25},
  {220, 100, 3.5, 11.5},
};
inline constexpr int kArmPtN = (int)(sizeof(kArmPts) / sizeof(kArmPts[0]));