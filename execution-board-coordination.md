# 执行板（STM32 小车/机械臂）改进需求协调文档

> 本文档给负责执行板固件的同事看。目标：让视觉大脑板（ESP32-CAM，AI 闭环）能感知执行板状态、确认动作完成，从而支撑「环境感知 / 快速反应 / 临场避障」三项能力。当前大脑板对执行板**完全盲视**：只下发指令、不读状态（0x0A 解析后仅打日志）。⚠️ 执行板**无测距传感器**（只有轮子+机械臂），避障只能靠大脑板视觉，详见 P1-2。

## 现状协议（架构文档 §5.4/§5.5）

| 方向 | CMD | 载荷 | 说明 |
| --- | --- | --- | --- |
| 大脑板 → 执行板 | 0x06 | — | 状态查询，执行板回 0x0A |
| 执行板 → 大脑板 | 0x0A | 小车: state(1) speed(1) param(2) flag(1) | state: 0 停止/1 移动中/2 转动中；param: 累计距离 cm 或角度；flag: bit0 打滑/堵转 |
| 执行板 → 大脑板 | 0x0A | 机械臂: state(1) grip(1) param(2) flag(1) | state: 0 空闲/1 升降中/2 移爪中；grip: 0 松/1 夹；param: 累计距离 |

## 硬件现状说明

> 执行板（小车）**没有红外/超声等测距传感器**，只有轮子和机械臂。因此「测障碍」「避险」这类能力**无法由执行板本地提供**，只能靠大脑板的视觉（2D 画面，周期约 1-5s/轮）。下述 P0-1 是**可选/未来**项——只有执行板后续加装测距传感器才成立；纯传感器方案见 P1-2 的替代说明。

## 需求清单（按优先级）

### P0-1（可选/未来）执行板本地急停 —— 需先加装测距传感器

- **背景**：视觉 AI 闭环真实周期约 1-5s/轮，若加装传感器，撞墙/撞人可更快刹车；大脑板软兜底仍是主防线。
- **前置**：执行板**当前无测距传感器**，本项仅在后续加装超声/红外后才适用，目前**不用做**。
- **若加装**：距离 < 15cm（可配置）时执行板自行急停（不等待大脑板指令），并上行事件帧通知大脑板。
- **上行格式**：复用 0x0A flag bit 置位（如 bit1=避障急停），或新 CMD 0x0B `obstacle_cm(2) + flag(1)`。
- **大脑板侧用途**：收到后立即停当前 AI 任务、把「前方 XX cm 有障碍」拼进下一轮 AI 上下文供绕行决策。

### P0-2 状态帧改为周期主动上报

- **背景**：现在 0x0A 是查询应答（0x06 驱动），大脑板每轮 AI 决策前要额外发一次查询才有数据，且拿不到连续状态。架构文档 §5.5 已允许周期上报，请落地。
- **要求**：执行板每 **100-500ms** 主动上行一帧 0x0A（不依赖查询）。建议小车/机械臂各一条，或按频率合并。
- **字段**：沿用现有 `state/speed/grip/param/flag`，语义以执行板为准。
- **大脑板侧用途**：把「执行板状态：小车移动中/已停止、夹爪已夹住、打滑 flag」拼进每轮 AI user 消息 → AI 不再全靠画面猜运动状态（这是「良好感知小车运动状态」的核心依赖）。
- **大脑板侧进度**：已实现 `uart::read_state()` 缓存 + AI 上下文拼接（`uart.cpp`/`ai_client.cpp`），执行板开始周期上报后**自动生效**，无需再改大脑板。

### P1-1 指令执行完成标志

- **背景**：定距/定角/夹爪指令发出后，大脑板不知道执行板何时做完。若间隔缩小（AI 已降到 500ms 防抖），新指令可能在上一条未走完时覆盖，造成抖动。
- **要求**：定距/定角/夹爪类指令**完成**时回一帧完成状态。建议复用 0x0A：`state=0` 且新增 `done(1)` 字节（0=执行中/1=本指令已完成），或新 CMD 0x0B 事件帧。
- **大脑板侧用途**：收到 done 前不催下一步（AI 上下文里"上一步已下发"配合完成标志，避免重复/覆盖指令）。

### P1-2 避障：大脑板视觉为主（执行板无测距传感器）

- **背景**：执行板**没有**超声/红外等测距硬件，避障**只能由大脑板视觉完成**（2D 画面识别障碍物，通过回传的视角判断绕行/急停）。本项**不需要执行板改动**，仅作说明。
- **大脑板现状**：提示词已引导「障碍物挡路则尝试绕行；绕行多轮仍无进展才 stop」（ai_client.cpp）；`stop scope` 支持 wheels/arm/all 分级停车。跨帧视觉遮挡判断逻辑在 `ai_client` 内部（见 "上一步已下发"+多轮无进展引导的防线）。
- **若未来加装测距**：周期上报 `obstacle_cm(2)`（无传感器置 0xFFFF），可与 P0-2 合并进 0x0A 或走事件帧；大脑板收到后距离 < 阈值 → 代码层硬性短路 `uart::act("stop")`（不经过 AI）。

### P1-3 夹爪状态细化（握持物交接确认）

- **背景**：现有 `grip(1)`（0 松/1 夹）不够支撑「夹没夹到东西」的交接确认，AI 无法判断 release 后交接是否成功。
- **要求**：增加 `has_load(1)`（是否夹到东西，可用夹爪电流/到位开关判定）与 `grip_done(1)`（夹紧/松开动作已完成）。
- **大脑板侧用途**：AI 提示词已含交接语义（"先停稳、伸到其手边再 release"）；有 has_load/grip_done 后 AI 可确认「交接成功/需重试」。

### P2 查询接口保留

- 0x06 查询保留作为兜底（执行板暂不做周期上报时，大脑板可退化为每轮决策前查询一次，语义一致，多一次 UART 往返可忽略）。

## 变更影响

- 所有新字段**优先复用 0x0A 帧 + 扩展字节/flag 位**，尽量避免新增 CMD，减少大脑板与执行板两侧协议漂移风险。
- 大脑板侧解析（`uart.cpp`）会同步跟进，两侧改动需要一起联调。
- 有任何字段语义不确定处，以执行板固件实际实现为准（架构文档 §5.5 注释已注明「flag/故障语义待定」）。
