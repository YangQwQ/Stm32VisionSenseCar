/*
 * 宿主端测试用 stm32f10x.h 桩头文件（仅提供类型定义，无任何寄存器/外设）。
 * 仅用于在 PC 上以 MinGW gcc 编译运行协议层单测；不烧录，不进入真实固件。
 */
#ifndef _STM32F10X_STUB_H_
#define _STM32F10X_STUB_H_

#include <stdint.h>
#include <stddef.h>

#endif /* _STM32F10X_STUB_H_ */